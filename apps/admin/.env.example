VITE_WORKOS_CLIENT_ID=
# VITE_API_BASE_URL=http://localhost:8787    # uncomment + set for local dev against wrangler
# VITE_WEB_URL=                              # override the "back to site" link target (prod → https://usrse.org, dev → http://localhost:5173)
